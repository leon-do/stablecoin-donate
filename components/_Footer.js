const footerStyle = {
  backgroundColor: "red",
  color: "white",
  width: "100%",
  height: "60px",
};

const NavBar = () => (
  <footer className="footer" style={footerStyle}>
    FOOTER
  </footer>
);

export default NavBar;
